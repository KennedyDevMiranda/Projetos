using Central_do_Educador;
using Central_do_Educador.Forms;
using System;
using System.Windows.Forms;

internal static class Program
{
    [STAThread]
    static void Main()
    {
        ApplicationConfiguration.Initialize();
        Central_do_Educador.Data.Db.InitializeAsync().GetAwaiter().GetResult();
        Application.Run(new FrmLogin());
        Application.ApplicationExit += (s, e) => WhatsAppDriverManager.Encerrar();
    }
}